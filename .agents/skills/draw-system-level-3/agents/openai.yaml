interface:
  display_name: "Draw System · Nível 3"
  short_description: "Plano de execução cirúrgico (tasks de implementação)."
  default_prompt: "Use $draw-system-level-3 para estruturar as tasks de implementação no caminho crítico de código."
policy:
  allow_implicit_invocation: true
